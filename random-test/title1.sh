#!/usr/bin/env bash
. color.sh 
printf '%s\n' ${BLUE}"==========================="${NORMAL}
printf '%s\n' ${YELLOW}${UNDERLINE}"Installing an AUR Helper"${NORMAL}
printf '%s\n' ${BLUE}"==========================="${NORMAL}
